services:
  - type: web
    name: quizball
    runtime: docker
    plan: free
    branch: claude/football-quiz-game-x668h
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: HOSTNAME
        value: 0.0.0.0
